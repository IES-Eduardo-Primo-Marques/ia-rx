FROM python:3.11-slim

WORKDIR /app

# Uncomment and copy requirements when ready
# COPY requirements.txt .
# RUN pip install --no-cache-dir -r requirements.txt

COPY . .

# Adjust the command based on your framework (FastAPI/Flask)
CMD ["python", "main.py"]
